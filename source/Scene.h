#pragma once

//first node of hierarchy(store other nodes (objects, for example: chunkMap, UserInterface etc)
class Scene 
{
public:
	Scene();
	~Scene();
	//void AddGameObject(GameObject gameObject);
private:
	//std::vector<GameObject> childs;
};

